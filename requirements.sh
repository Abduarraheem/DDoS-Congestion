#! /bin/bash

sudo apt-get update
# to compile/run CPP files
sudo apt-get install g++
# for client/server connection 
sudo apt-get install iperf 
# allows user to set up static IPS
sudo apt-get install ifupdown net-tools

sudo apt install cmake
